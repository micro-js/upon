{
  "name": "@f/upon",
  "description": "Run a function upon some arguments",
  "repository": "git://github.com/micro-js/upon.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/slice": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
